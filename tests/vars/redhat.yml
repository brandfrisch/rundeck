---
goss:
  package:
    rundeck:
      installed: true
  service:
    rundeckd:
      enabled: true
      running: true
  user:
    rundeck:
      exists: true
      groups:
      - rundeck
      home: /var/lib/rundeck
      shell: /bin/bash
  group:
    rundeck:
      exists: true
